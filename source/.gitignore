# ==========================================
# Keil / MDK-ARM 忽略文件配置
# ==========================================
.vscode
# 1. 编译中间文件与输出目录
# CubeMX 生成的工程通常包含这两个文件夹，或者在工程根目录下生成
Objects/
Listings/
*.o
*.d
*.crf
*.dep

# 2. 编译输出产物 (固件文件)
# 建议：通常不建议将 .hex/.bin 提交到 Git，应由 CI/CD 自动生成或本地编译
# 如果你需要提交固件给测试人员，请注释掉下面这几行
*.hex
*.bin
*.axf
*.map
*.htm
*.lst

# 3. Keil 用户特定配置 (非常重要)
# 这些文件包含你本地的调试器设置（如 J-Link/ST-Link 配置）和窗口布局
# 提交它们会导致团队成员间的配置冲突
*.uvoptx
*.uvguix.*
*.uvprojx.user

# 4. 临时文件与备份
*.bak
*.tmp
*.log
JLinkLog.txt

clean.bat
*.__i

# 5. 第三方库与包 (视情况而定)
# 如果使用了 Keil Pack 管理器，建议忽略 Pack 目录，在 README 中说明依赖
# Packs/