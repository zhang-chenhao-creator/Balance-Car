# 仓库安全审计（2026-07-13）

## 审计范围

- 本地工作树、全部本地与远端分支、当前 Pull Request；
- 当前公开分支中的凭据、本机路径、身份资料和不应公开的构建产物；
- GitHub 仓库可见性、分支保护、Actions 权限、Secret Scanning、Push Protection、Dependabot 和 Code Scanning；
- EX5 固件校验、工程文件引用和不依赖硬件的安全监督测试。

## 已验证事实

- 仓库为公开仓库，默认分支为 `main`。
- `main` 与 `codex/ex5-ultrasonic-guard` 使用清理后的新历史；当前 EX5 Pull Request 可合并，但尚无自动检查结果。
- 当前分支的公开仓库扫描通过，未发现已配置规则覆盖的凭据、本机用户路径或微信路径。
- GitHub Secret Scanning 与 Push Protection 已启用，当前无开放的 Secret Scanning 告警。
- Actions 默认令牌权限为只读，工作流不能批准 Pull Request；这是合适的最小权限配置。
- `main` 当前没有分支保护或 ruleset，Actions 允许所有外部 Action，且未要求完整提交 SHA。
- Dependabot alerts、Dependabot security updates 与 CodeQL default setup 当前未启用。
- 远端 `agent/add-ex5-ultrasonic-avoidance` 仍引用清理前的旧历史。该历史包含身份资料、提交者私人邮箱、本机绝对路径、微信文件路径、实验报告和备份固件，不能合并回新历史。
- EX5 固件 SHA-256 与 `FIRMWARE_SHA256SUMS.txt` 一致。
- EX5 安全监督 GCC 测试通过；Keil 工程的 31 个文件引用均存在；Python 工具语法检查通过。
- 已提交的 EX5 构建日志记录为 0 Error、0 Warning，但本机未安装 Keil，本次未重新执行 Keil 全量编译。
- 未执行烧录和实车测试，硬件结果仍为「待实车确认」。

## 本次本地加固

- 新增 GitHub Actions 工作流，在 Pull Request 和 `main` 推送时执行公开仓库扫描、固件哈希校验和 EX5 安全监督测试。
- 工作流只授予 `contents: read`，第三方 Action 固定到官方 release 对应的完整提交 SHA。
- 扫描器新增对 `.exe`、`.pyc`、Stripe 密钥、JWT 和带凭据数据库 URL 的检查。

## 待确认的远端动作

以下动作会改变 GitHub 状态，执行前必须得到用户确认：

1. 提交并推送本次本地加固修改到当前 Pull Request 分支。
2. 删除远端旧分支 `agent/add-ex5-ultrasonic-avoidance`，避免旧历史继续由分支引用。
3. 启用 Dependabot alerts、Dependabot security updates 和 CodeQL default setup。
4. 为 `main` 增加保护规则：禁止强推与删除、要求 Pull Request、要求安全工作流通过后才能合并。
5. 在检查通过后合并当前 Pull Request；合并后再拉取并验证本地 `main`。

删除远端分支只能解除该分支引用，不能保证 GitHub 缓存、Fork 或既有克隆中的旧对象立即消失。若需要彻底清除，应按 GitHub 的敏感数据移除流程检查其他引用，并视情况联系 GitHub Support 清理缓存视图。
