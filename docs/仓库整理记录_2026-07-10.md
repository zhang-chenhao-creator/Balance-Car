# 仓库整理记录（2026-07-10）

## 整理结论

- 整理前 GitHub `main`：`763a061e46c932b181c37a8f625c1b1a91cd25ff`。
- 整理前 GitHub EX3 不是 `实验三平衡串口精简版.zip`。
- 旧 EX3 的主结构是 `Drivers/`、`Middlewares/`、`Output/`、`Projects/`、`Users/`，还混入了 EX2 采集脚本、历史备份、实验数据、报告和构建产物。
- 指定 ZIP 的主结构是 `source/`、`docs/`、`tools/`、`backup/`，其源码基于 WHEELTEC B585 标准库工程，结构和核心入口均与旧 EX3 不同。
- 新 EX3 已整体使用指定 ZIP 替换，旧 EX3 不再作为代码主线。

## ZIP 核对

- 文件：`实验三平衡串口精简版.zip`
- SHA-256：`858405FDB6117224A801496F99D7C6812F4F5767E469E1968BE9503295AE1CD9`
- ZIP 内文件数：243。
- 导入后逐文件 SHA-256 核对：242 个文件内容完全一致。
- 唯一有意修改的文件：ZIP 中的 `00_先看我_平衡串口精简版.md` 改名为 `EX3/README.md`，并补充仓库主线、固件目录和验证边界说明。
- 另外从 ZIP 自带构建结果复制 `MiniBalance.hex`、`MiniBalance.bin` 到 `EX3/firmware/`，便于直接定位最终固件。

## 新目录边界

- `EX1`–`EX4`：可复现实验工程、文档、数据和最终固件。
- `docs/`：项目级背景、知识点和整理记录。
- `_local/`：原厂参考源码、课程资料、烧录软件、Ghidra/JDK 等，仅本地保留并由 `.gitignore` 排除。
- `Output/`、`OBJ/`、`backup/`、`运行记录/`：本地生成内容，不进入新仓库历史。
- EX0 已移除。

## 备份与验证边界

整理前已在用户文档目录创建旧 Git 历史 bundle、旧 EX3 压缩备份和 EX0 压缩备份。此次核对覆盖文件结构、来源与哈希；未连接实体小车重新执行硬件验收。

## 本地验证结果

- 四个 Keil 工程入口文件均存在，且未被 `.gitignore` 排除。
- 四份现有 Keil 构建日志均记录 `0 Error(s)`、`0 Warning(s)`。
- 当前环境的命令行 `PATH` 中没有 `UV4.exe`，因此本次未重新编译；上述构建日志属于整理前已有构建结果。
- 七个整理后固件与各自本地构建目录中的原文件 SHA-256 完全一致，校验值见仓库根目录 `FIRMWARE_SHA256SUMS.txt`。
- README 中的本地相对链接已逐项检查，未发现失效链接。
- 隐私清理后新起点包含 607 个候选文件，合计约 29.62 MiB；最大单文件约 2.91 MiB，低于 GitHub 单文件限制。
- EX1/EX2 实验报告含姓名、学号和文档作者元数据，已整体转移到仅本地 `_local/实验报告/`。
- EX2 报告生成脚本已取消硬编码身份信息，改为通过命令行参数传入；面试笔记中的个人报告路径和旧 EX3 路径已更新。
- 发布候选文件经过高置信凭据与身份信息复扫，未发现命中；自动复查入口为 `scripts/check_public_repo.ps1`。
