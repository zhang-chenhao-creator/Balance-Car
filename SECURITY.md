# 公开仓库边界与安全检查

本仓库是公开仓库。目标是公开可复现的实验工程，同时把身份信息、凭据、本机软件和不可复现的大文件留在本地。

## 可以公开

- EX1–EX4 的源码与 Keil 项目配置；
- 已匿名化的技术说明、实验目标和调参资料；
- 不含身份信息的传感器实验数据；
- 最终 `.hex` / `.bin` 固件及 SHA-256 校验值。

## 仅本地保存

- `_local/` 下的课程资料、原厂参考源码、烧录软件和开发工具；
- 含姓名、学号、作者属性或其他身份信息的实验报告；
- Keil 构建目录、烧录备份、运行日志、历史版本和本机配置；
- `.env`、私钥、证书、credentials/secrets 配置等任何凭据文件。

## 凭据规则

- API Key、Token、密码和私钥不得写入源码、README、脚本默认值或 Git 历史。
- 需要凭据时使用环境变量或位于 `_local/` 的本机配置，并提供不含真实值的 `.env.example`。
- 如果真实凭据曾进入公开历史，应先在服务商处撤销或轮换，再重写历史；仅删除当前文件不能使旧凭据失效。

## 2026-07-10 发布前检查

- 高置信规则扫描未发现 OpenAI、GitHub、AWS、Google、Slack、Stripe Token、私钥、JWT 或带凭据的数据库 URL。
- GitHub Secret Scanning 当前无开放告警。
- 发现的姓名、学号和 Office/PDF 作者元数据位于实验报告、报告生成脚本默认值和旧面试笔记路径中；实验报告已转移到 `_local/`，脚本已改为运行时参数，笔记路径已匿名化并更新。
- 远端没有 Pull Request 或 Fork 引用旧历史。

自动检查脚本：`scripts/check_public_repo.ps1`。

